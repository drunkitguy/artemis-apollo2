<?xml version="1.0" encoding="utf-8"?>
<!-- Small circular chip used behind corner badges (e.g. the padlock). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/vl_card" />
    <stroke
        android:width="1dp"
        android:color="@color/vl_separator" />
</shape>
