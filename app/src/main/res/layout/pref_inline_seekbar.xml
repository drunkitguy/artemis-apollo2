<?xml version="1.0" encoding="utf-8"?>
<!--
    A slider that lives in the list rather than behind a dialog: current value on
    the title row, endpoints under the track, explanation underneath.

    The endpoints matter. A bare slider tells you nothing about what range you
    are moving through, so "is 5 a lot?" needs a dialog to answer. With min and
    max printed it does not.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="@dimen/settings_h_padding"
    android:paddingEnd="@dimen/settings_h_padding"
    android:paddingTop="@dimen/settings_v_padding"
    android:paddingBottom="@dimen/settings_v_padding">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/seekbar_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="@color/settings_text_primary"
            android:textSize="@dimen/settings_title_text" />

        <TextView
            android:id="@+id/seekbar_value"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:textColor="@color/settings_value"
            android:textSize="@dimen/settings_value_text"
            android:textStyle="bold" />
    </LinearLayout>

    <SeekBar
        android:id="@+id/seekbar_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:minHeight="@dimen/settings_row_min_height"
        android:paddingStart="0dp"
        android:paddingEnd="0dp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/seekbar_min"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="@color/settings_text_hint"
            android:textSize="@dimen/settings_summary_text" />

        <TextView
            android:id="@+id/seekbar_max"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="end"
            android:textColor="@color/settings_text_hint"
            android:textSize="@dimen/settings_summary_text" />
    </LinearLayout>

    <TextView
        android:id="@+id/seekbar_explain"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:textColor="@color/settings_text_secondary"
        android:textSize="@dimen/settings_summary_text"
        android:visibility="gone" />

</LinearLayout>
